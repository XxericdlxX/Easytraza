/**
 * Controladors web i API del backend d'EasyTraza.
 *
 * <p>
 * Aquest package agrupa les classes encarregades de rebre les peticions HTTP,
 * preparar les dades necessàries per a les vistes Thymeleaf o les respostes API,
 * i delegar la lògica de negoci als serveis corresponents.
 * </p>
 *
 * <p>
 * Inclou controladors per a clients, proveïdors, productes, matèries primeres,
 * lots, albarans, comandes, autenticació i funcionalitats de suport.
 * </p>
 */
package cat.copernic.easytraza_backend.controller;
