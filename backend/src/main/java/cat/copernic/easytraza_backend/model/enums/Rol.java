package cat.copernic.easytraza_backend.model.enums;

/**
 * Entitat del model `Rol` utilitzada pel domini d'EasyTraza.
 */
public enum Rol {
    /**
     * Valor `OPERARI` utilitzat per representar l'estat operari.
     */
    OPERARI,
    /**
     * Valor `ADMIN` utilitzat per representar l'estat admin.
     */
    ADMIN
}
