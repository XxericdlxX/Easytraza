package cat.copernic.easytraza_backend.model.enums;

/**
 * Entitat del model `TipusClient` utilitzada pel domini d'EasyTraza.
 */
public enum TipusClient {
    /**
     * Valor `RESTAURANT` utilitzat per representar l'estat restaurant.
     */
    RESTAURANT,
    /**
     * Valor `PANADERIA` utilitzat per representar l'estat panaderia.
     */
    PANADERIA,
    /**
     * Valor `PASTISSERIA` utilitzat per representar l'estat pastisseria.
     */
    PASTISSERIA,
    /**
     * Valor `ALTRES` utilitzat per representar l'estat de altres.
     */
    ALTRES
}
