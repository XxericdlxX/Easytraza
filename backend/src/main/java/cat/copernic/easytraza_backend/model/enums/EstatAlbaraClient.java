package cat.copernic.easytraza_backend.model.enums;

/**
 * Entitat del model `EstatAlbaraClient` utilitzada pel domini d'EasyTraza.
 */
public enum EstatAlbaraClient {
    /**
     * Valor `PENDENT_LLIURAR` utilitzat per representar l'estat pendent
     * lliurar.
     */
    PENDENT_LLIURAR,
     /**
     * Valor `LLIURAT` utilitzat per representar l'estat lliurat
     */
    LLIURAT
}
